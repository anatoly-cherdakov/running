const {chromium}=require('playwright');
(async()=>{const b=await chromium.launch({headless:true});const p=await b.newPage({viewport:{width:1440,height:1000}});const errors=[];const fails=[];p.on('pageerror',e=>errors.push(e.message));p.on('requestfailed',r=>fails.push([r.url(),r.failure()?.errorText]));await p.goto('https://anatoly-cherdakov.github.io/running/',{waitUntil:'domcontentloaded',timeout:60000});await p.waitForSelector('#leaflet-map .leaflet-tile',{timeout:60000});await p.waitForTimeout(3000);for(let i=0;i<5;i++){let x=await p.evaluate(()=>{let e=document.querySelector('#leaflet-map'),r=e.getBoundingClientRect();return {rect:[r.width,r.height],tiles:e.querySelectorAll('.leaflet-tile').length,markers:e.querySelectorAll('.leaflet-marker-icon').length,display:getComputedStyle(e).display,vis:getComputedStyle(e).visibility}});console.log(i,x);await p.locator('.leaflet-control-zoom-in').click();await p.waitForTimeout(2500);}console.log({errors,fails:fails.slice(0,20)});if(errors.length)process.exit(1);await b.close();})();
